name: Lab - k3s etcd cold import replay

on:
  workflow_dispatch:
    inputs:
      k3s_channel:
        description: "k3s install channel"
        required: true
        default: "stable"
        type: choice
        options:
          - stable
          - latest
      etcd_version:
        description: "Standalone etcd release"
        required: true
        default: "v3.5.21"
      object_count:
        description: "Synthetic Kubernetes object count per namespace"
        required: true
        default: "20"
      conflict_policy:
        description: "Load conflict policy for replay"
        required: true
        default: "fail-if-present"
        type: choice
        options:
          - fail-if-present
          - allow-identical-replay
      upload_raw_etcd_artifacts:
        description: "Dangerous: upload raw etcd snapshot/dumps containing Kubernetes Secrets"
        required: true
        default: false
        type: boolean

permissions:
  contents: read

concurrency:
  group: lab-k3s-etcd-cold-import-replay
  cancel-in-progress: false

jobs:
  lab:
    name: k3s embedded etcd cold import replay/idempotence
    runs-on: ubuntu-24.04
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Build migrator
        run: |
          set -euo pipefail
          go build -o bin/etcd-migrator ./cmd/etcd-migrator
          ./bin/etcd-migrator --help || true

      - name: Run k3s etcd cold-import replay lab
        env:
          K3S_CHANNEL: ${{ inputs.k3s_channel }}
          ETCD_VERSION: ${{ inputs.etcd_version }}
          OBJECT_COUNT: ${{ inputs.object_count }}
          CONFLICT_POLICY: ${{ inputs.conflict_policy }}
          UPLOAD_RAW_ETCD_ARTIFACTS: ${{ inputs.upload_raw_etcd_artifacts }}
        run: |
          set -euo pipefail
          sudo --preserve-env=K3S_CHANNEL,ETCD_VERSION,OBJECT_COUNT,CONFLICT_POLICY,UPLOAD_RAW_ETCD_ARTIFACTS \
            bash scripts/lab_k3s_etcd_cold_import_replay.sh

      - name: Verify lab artifacts
        run: |
          set -euo pipefail

          lab_dir="$(find runs -maxdepth 1 -type d -name 'lab-k3s-etcd-cold-import-replay-*' | sort | tail -1)"
          if [[ -z "$lab_dir" ]]; then
            echo "ERROR: No lab directory found" >&2
            exit 1
          fi

          artifact_dir="$lab_dir/artifacts"
          if [[ ! -d "$artifact_dir" ]]; then
            echo "ERROR: Artifact directory not found: $artifact_dir" >&2
            exit 1
          fi

          echo "Verifying artifacts in: $artifact_dir"
          bash scripts/verify_k3s_etcd_cold_import_replay_artifact.sh "$artifact_dir"

      - name: Upload safe lab evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: k3s-etcd-cold-import-replay-lab
          path: |
            runs/lab-k3s-etcd-cold-import-replay-*/artifacts/**
            !runs/lab-k3s-etcd-cold-import-replay-*/artifacts/**/*.db
            !runs/lab-k3s-etcd-cold-import-replay-*/artifacts/**/*.jsonl
            !runs/lab-k3s-etcd-cold-import-replay-*/artifacts/**/*.kv.tsv
            !runs/lab-k3s-etcd-cold-import-replay-*/artifacts/**/*kv*.tsv
          if-no-files-found: warn

      - name: Upload raw lab artifacts (opt-in)
        if: always() && inputs.upload_raw_etcd_artifacts
        uses: actions/upload-artifact@v4
        with:
          name: k3s-etcd-cold-import-replay-raw
          path: |
            runs/lab-k3s-etcd-cold-import-replay-*/raw-artifacts/**
          if-no-files-found: warn
